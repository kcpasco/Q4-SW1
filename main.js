// declare variables
const u, p;
u = "admin"; //string data type
p = "12345"; //string data type

// add the function 
function verbNoun() {
    let userNameInput = document.getElementById("UserName").value;
    let passCodeInput = 
    let fb = 

    // test by using if-else
    if (u === userNameInput && p === passCodeInput) {
        // feedback if username and password are correct

    } else  (u === userNameInput && p === passCodeInput) {
        // feedback if username and password are incorrect
    }
}
